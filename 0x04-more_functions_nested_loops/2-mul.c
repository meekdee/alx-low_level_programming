#include "main.h"

/**
 * mul- multiplies two digits
 * @a: the character to be multiplied
 * @b: the character to be multiplied
 * Return: the results
 */
int mul(int a, int b)
{
	int m = a * b;

	return (m);
}
